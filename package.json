{
  "author": "Sebastian Wiedenroth <wiedi@frubar.net>",
  "contributors": [
    {
      "email": "mail@b-vladi.ru",
      "name": "Vlad Kurkin"
    }
  ],
  "dependencies": {
    "sacn": "^4.4.0",
    "serialport": "^12.0.0",
    "socket.io-client": "^4.7.5"
  },
  "description": "A nodejs DMX library",
  "devDependencies": {
    "@types/eslint__js": "^8.42.3",
    "@vk/dmx-types": "github:vs-kurkin/dmx-types#0.0.14",
    "concurrently": "^8.2.2",
    "eslint": "^8.57.0",
    "eslint-plugin-jest": "^28.5.0",
    "globals": "^15.3.0",
    "jest": "^29.7.0",
    "npm-check": "^6.0.1",
    "typescript": "^5.4.5",
    "typescript-eslint": "^7.12.0"
  },
  "imports": {
    "#": "./src/index.js"
  },
  "files": [
    "src",
    "types"
  ],
  "keywords": [
    "DMX",
    "light control"
  ],
  "license": "MIT",
  "main": "./src/index.js",
  "name": "@vk/dmx",
  "scripts": {
    "audit": "npm audit fix",
    "build": "npx tsc",
    "check": "npm install && npm run build && npm run lint",
    "doctor": "npm doctor",
    "format": "npm run test:lint -- --fix",
    "lint": "npx eslint .",
    "prerelease": "npm run check && git commit -m \"Release %npm_package_version%\" -a",
    "release": "git tag -a %npm_package_version% -f -m \"Release %npm_package_version%\" && git push && git push --tags",
    "test": "npx concurrently 'npm run test:lint' 'npm run test:jest'",
    "unit": "npx jest",
    "update": "npm update --save"
  },
  "type": "module",
  "types": "./types/index.d.ts",
  "version": "0.12.0"
}
